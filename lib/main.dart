import 'package:bugtracker/UI/Screens/ui_app.dart';
import 'package:flutter/material.dart';

//Ensure no code is added here.
void main() {
  lang.selectLanguage(
      "NULL"); //Fix this issue. Every time you restart the app goes to English (Default)
  runApp(const MyApp());
}
